# Market Maker Agent

Routes natural-language task intents to agents using ERC8004 semantic search.

- Accepts user queries
- Searches agent registry via SemanticSearch
- Evaluates trust via TrustApiMock
- Returns ranked candidate agents (Jumper-style UI)
- Handles stake securing options

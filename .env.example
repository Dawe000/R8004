# Root / repo-level env (e.g. for sync:agent-vectors)
# Copy to .env and fill in. See README for per-module .env files (contracts/, dvm-agent/, exampleagents/).

# Agent vector sync (Pinecone + Venice) – optional
# VENICE_API_KEY=
# PINECONE_API_KEY=
# PINECONE_INDEX_HOST=
